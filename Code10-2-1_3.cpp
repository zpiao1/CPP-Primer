#include <algorithm>
#include <numeric>
using namespace std;
int main()
{
    // roster2 should have at least as many elements as roast1
    equal(roster1.cbegin(), roster2.cend(), roster2.cbegin());
    return 0;
}