#include <iostream>
#include <string>
#include <vector>
using namespace std;
int main()
{
    // read from standard input, putting each word onto the end of container
    string word;
    while (cin >> word)
        container.push_back(word);
}