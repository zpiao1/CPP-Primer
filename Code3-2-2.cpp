#include <iostream>
#include <string>
using namespace std;
// Note: #include and using declarations must be added to compile this code
int main()
{
    string s;           // empty string
    cin >> s;           // read a whilespace-separated string into s
    cout << s << endl;  // write s to the output
    return 0;
}
