#include <iostream>
#include <vector>
#include <string>
using namespace std;
int main()
{
    vector<int> ivec(10);           // ten int elements, each initialized 0
    vector<string> svec(10);        // ten elements, each an empty string
    return 0;
}


